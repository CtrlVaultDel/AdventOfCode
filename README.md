# Advent of Code

Repository for housing the solutions to Advent of Code challenges for each year

**Note**
Currently running solutions through one of two methods:
1. CodeRunner Extension (VSC) -> Open desired solution.mjs file -> (Ctrl + Alt + N)
2. Open terminal -> cd to the desired day -> type: node solution.mjs